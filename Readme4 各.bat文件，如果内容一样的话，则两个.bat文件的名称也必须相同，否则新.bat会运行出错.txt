Readme4 各.bat文件，如果内容一样的话，则两个.bat文件的名称也必须相同，否则新.bat会运行出错

意思是：

比如【bat文件中】的 2 和 6 内容完全一样，如果6 后面的名称和2不一样的话，则运行2 没问题，运行6则出错，或其实不push本地文件到Github终端。

2 同步本地到终端 hexo update.bat

6 同步本地到终端 hexo update.bat

But why？ I don't Know...